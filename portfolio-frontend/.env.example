# Base URL of the backend API (see ../portfolio-backend). Leave empty during
# local dev if you're using the Vite proxy; set to your deployed backend URL
# in production (e.g. https://your-backend.vercel.app).
VITE_BACKEND_URL=http://localhost:8000
